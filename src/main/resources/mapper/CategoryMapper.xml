<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.coding24h.mall_spring.mapper.CategoryMapper">

    <!-- 结果映射 -->
    <resultMap id="CategoryResultMap" type="com.coding24h.mall_spring.entity.Category">
        <id property="categoryId" column="category_id"/>
        <result property="categoryName" column="category_name"/>
        <result property="parentId" column="parent_id"/>
        <result property="icon" column="icon"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="isShow" column="is_show"/>
        <result property="attributeTemplate" column="attribute_template"/>
    </resultMap>

    <!-- 根据ID查询分类 -->
    <select id="selectById" parameterType="java.lang.Integer" resultMap="CategoryResultMap">
        SELECT
            category_id,
            category_name,
            parent_id,
            icon,
            sort_order,
            is_show,
            attribute_template
        FROM product_categories
        WHERE category_id = #{categoryId}
    </select>

</mapper>
